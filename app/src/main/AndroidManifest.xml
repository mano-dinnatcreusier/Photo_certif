<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        CAMERA : nécessaire pour accéder à l'objectif physique.
        Déclarée comme "required" pour que l'app ne s'installe pas sur les appareils sans caméra.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!--
        Pas besoin de READ_EXTERNAL_STORAGE / WRITE_EXTERNAL_STORAGE :
        on sauvegarde dans getExternalFilesDir() qui est privé à l'application
        (scoped storage, Android 10+). Sur Android 8-9 c'est également accessible
        sans permission car c'est le dossier spécifique de l'app.
    -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@android:mipmap/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhotoCertif">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
